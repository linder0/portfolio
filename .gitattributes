# Only keep files >100MB in LFS (GitHub limit)
# These 3 projects need external hosting or LFS-enabled deploy

# Files that MUST stay in LFS (>100MB)
public/videos/nanostalgia/nanostalgia.mp4 filter=lfs diff=lfs merge=lfs -text
public/videos/petri-dishes/petri-dishes.mp4 filter=lfs diff=lfs merge=lfs -text
public/videos/chameleon/assembly.MOV filter=lfs diff=lfs merge=lfs -text

# All other media files commit directly (Vercel compatible)
